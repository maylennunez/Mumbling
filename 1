function accum(s) {
let letters = s.toLowerCase().split('');

  for(let i = 0; i < letters.length; i++) {
    
    letters[i] = letters[i].toUpperCase() + letters[i].repeat(i);
    
    let result= (letters[i])
  }
  return letters.join('-')
}

accum('maylen');
